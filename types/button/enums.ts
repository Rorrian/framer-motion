export const enum Size {
	Big = "big",
	Small = "small",
}
export const enum Kind {
	Primary = "primary",
	Secondary = "secondary",

	Positive = "positive",
	Negative = "negative",

	Transparent = "transparent",
}

export const enum Justify {
	Start = "start",
	Center = "center",
}
